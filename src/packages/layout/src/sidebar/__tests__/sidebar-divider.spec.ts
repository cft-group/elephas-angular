import {ESidebarDivider} from '../sidebar-divider';

describe('ESidebarDivider', (): void => {
    it('should create an instance', (): void => {
        const directive: ESidebarDivider = new ESidebarDivider();
        expect(directive).toBeTruthy();
    });
});

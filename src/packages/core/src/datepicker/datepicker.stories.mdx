import {Meta, Props, Preview, Story} from '@storybook/addon-docs/blocks';
import {moduleMetadata} from '@storybook/angular';
import {EInputModule} from '../input/input.module';
import {EDatepickerModule} from './datepicker.module';
import {EDatepicker} from './datepicker';

# Datepicker

<Meta title="Core/Datepicker" decorators={[moduleMetadata({ imports: [EInputModule, EDatepickerModule] })]} />

<Props of={EDatepicker} />

<Preview>
    <Story name="default" height="320px">
        {{
            template: `
                <e-base-input
                    [label]="label"
                    [width]="width">
                    <input eInput>
                    <e-datepicker></e-datepicker>
                </e-base-input>
            `,
            props: {
                label: 'Date',
                width: 4
            },
        }}
    </Story>
</Preview>

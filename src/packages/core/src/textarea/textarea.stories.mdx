import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { EInput, EInputModule } from '../input';
import { EBaseInput } from '../base-input/base-input';

# Textarea

<Meta title="Core/Textarea" decorators={[moduleMetadata({ imports: [EInputModule] })]} />

<Props of={EInput} />
<Props of={EBaseInput} />

<Preview>
    <Story name="default" height="200px">
        {{
            template: `
                <e-base-input
                    [label]="label"
                    [width]="width">
                    <textarea eTextarea [rows]="rows"></textarea>
                </e-base-input>
            `,
            props: {
                label: 'Комментарий',
                width: '8',
                rows: 6
            }
        }}
    </Story>
</Preview>

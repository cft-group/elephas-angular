import { EUploadDragAndDrop } from '../upload-drag-and-drop';

describe('Directive: EUpload', (): void => {
    it('should create an instance', (): void => {
        const directive: EUploadDragAndDrop = new EUploadDragAndDrop();
        expect(directive).toBeTruthy();
    });
});

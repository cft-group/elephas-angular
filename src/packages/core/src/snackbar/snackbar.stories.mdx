import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { ESnackbar } from './';
import { ESnackbarDemoModule } from './demo/demo.module';

# Snackbar

<Meta title="Core/Snackbar" decorators={[moduleMetadata({ imports: [ESnackbarDemoModule] })]} />

<Props of={ESnackbar} />

<Preview>
    <Story name="default" height="300px">
        {{
            template: `
                <e-snackbar-demo></e-snackbar-demo>
            `,
        }}
    </Story>
</Preview>


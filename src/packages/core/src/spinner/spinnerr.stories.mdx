import { Meta, Props, Preview, Story } from '@storybook/addon-docs/blocks';
import { moduleMetadata } from '@storybook/angular';

import { ESpinnerModule } from './';

# Spinner

<Meta title="Core/Spinner" decorators={[moduleMetadata({ imports: [ESpinnerModule] })]} />

<Preview>
    <Story name="default" height="100px">
        {{
            template: `
                <e-spinner></e-spinner>
            `,
        }}
    </Story>
</Preview>

